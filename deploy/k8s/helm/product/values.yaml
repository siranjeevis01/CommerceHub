replicaCount: 3
image:
  repository: ghcr.io/commercehub/commercehub/product-service
  tag: latest
  pullPolicy: Always
service:
  type: ClusterIP
  port: 8080
env:
  ASPNETCORE_ENVIRONMENT: Production
envFromSecrets:
  - PRODUCT_DB_CONNECTION
envFromConfigMap:
  - RABBITMQ_HOST
  - ELASTICSEARCH_URL
  - REDIS_CONNECTION_STRING
  - SEQ_URL
  - OTLP_EXPORTER_ENDPOINT
resources:
  requests: { memory: "512Mi", cpu: "250m" }
  limits: { memory: "1Gi", cpu: "500m" }
autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
podDisruptionBudget:
  enabled: true
  minAvailable: 2
affinity:
  podAntiAffinity:
    preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchExpressions:
              - key: app.kubernetes.io/name
                operator: In
                values: [product]
          topologyKey: kubernetes.io/hostname

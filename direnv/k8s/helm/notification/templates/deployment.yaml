apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "commercehub-notification.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "commercehub-notification.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "commercehub-notification.name" . }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "commercehub-notification.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          env:
            - name: SEQ_URL
              value: "http://seq:80"
            - name: JAEGER_AGENT_HOST
              value: "jaeger"
            - name: REDIS_CONNECTION_STRING
              value: {{ .Values.env.REDIS_CONNECTION_STRING }}
            - name: RABBITMQ_HOST
              value: {{ .Values.env.RABBITMQ_HOST }}
            - name: RABBITMQ_USER
              value: {{ .Values.env.RABBITMQ_USER }}
            - name: RABBITMQ_PASS
              value: {{ .Values.env.RABBITMQ_PASS }}
            - name: TWILIO_ACCOUNT_SID
              valueFrom:
                secretKeyRef:
                  name: commercehub-secrets
                  key: twilio-account-sid
            - name: TWILIO_AUTH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: commercehub-secrets
                  key: twilio-auth-token
            - name: TWILIO_PHONE_NUMBER
              valueFrom:
                secretKeyRef:
                  name: commercehub-secrets
                  key: twilio-phone-number
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health/live
              port: {{ .Values.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health/ready
              port: {{ .Values.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 15
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "commercehub-notification.fullname" . }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "commercehub-notification.fullname" . }}
  minReplicas: {{ .Values.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.targetCPUUtilizationPercentage }}

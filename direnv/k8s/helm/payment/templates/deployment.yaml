apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "commercehub-payment.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "commercehub-payment.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "commercehub-payment.name" . }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "commercehub-payment.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          env:
            - name: SEQ_URL
              value: "http://seq:80"
            - name: JAEGER_AGENT_HOST
              value: "jaeger"
            - name: PAYMENT_DB_CONNECTION
              value: {{ .Values.env.PAYMENT_DB_CONNECTION }}
            - name: RABBITMQ_HOST
              value: {{ .Values.env.RABBITMQ_HOST }}
            - name: RABBITMQ_USER
              value: {{ .Values.env.RABBITMQ_USER }}
            - name: RABBITMQ_PASS
              value: {{ .Values.env.RABBITMQ_PASS }}
            - name: STRIPE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: commercehub-secrets
                  key: stripe-secret-key
            - name: STRIPE_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: commercehub-secrets
                  key: stripe-webhook-secret
            - name: RAZORPAY_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: commercehub-secrets
                  key: razorpay-key-id
            - name: RAZORPAY_KEY_SECRET
              valueFrom:
                secretKeyRef:
                  name: commercehub-secrets
                  key: razorpay-key-secret
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health/live
              port: {{ .Values.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health/ready
              port: {{ .Values.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 15
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "commercehub-payment.fullname" . }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "commercehub-payment.fullname" . }}
  minReplicas: {{ .Values.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.targetCPUUtilizationPercentage }}
